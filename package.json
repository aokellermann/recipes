{
  "name": "recipe-backstory-proxy",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that strips recipes and keeps only the backstories",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251229.0",
    "wrangler": "^4.54.0"
  }
}
