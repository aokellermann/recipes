{
  "name": "the-scenic-route",
  "version": "1.0.0",
  "description": "Take the long way through recipe blogs - strips recipes, keeps the stories",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251229.0",
    "wrangler": "^4.54.0"
  }
}
